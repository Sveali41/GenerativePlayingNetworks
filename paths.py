import os
# class containing all the structures of the
class Paths:
    ROOT = os.path.dirname(__file__)
    LEVEL = os.path.join(ROOT, "level")
    LEVEL_FILE = os.path.join(ROOT, "level/lvl_0.txt")
